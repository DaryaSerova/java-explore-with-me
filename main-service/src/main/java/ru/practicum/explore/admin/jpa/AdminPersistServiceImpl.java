package ru.practicum.explore.admin.jpa;

import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Service;

@Service
@RequiredArgsConstructor
public class AdminPersistServiceImpl implements AdminPersistService {

//    @Override
//    public Page<Event> getEvents(List<Long> usersIds, List<StateEvent> states, List<Long> categories,
//                                 LocalDateTime rangeStart, LocalDateTime rangeEnd, int from, int size) {
//
//        return eventRepository.findEventByUserIdAndByStateAndByCategory(usersIds, states, categories,
//                                                                        rangeStart, rangeEnd,
//                                                                        PageRequest.of(from, size));
//    }
}
